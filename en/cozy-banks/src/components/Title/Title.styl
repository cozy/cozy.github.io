@require 'settings/breakpoints'

.Title
    line-height 2.5rem
    margin 0
    display flex
    align-items center
    color var(--black) !important

.TitleColor_inverted
    color var(--primaryContrastTextColor) !important

+small-screen()
    .Title
        font-size 1.125rem
        line-height 1.5rem
        margin 0 auto
